# AI费曼教室

把知识讲给 AI 学生，发现自己哪里没讲明白。

AI费曼教室是一个面向理工科学生的学习工作台。用户先输入学科，并在“知识点讲解 / 题目讲解”两种模式中选择一种；进入讲解工作区后，反复向 AI 学生讲解。系统不打分，只指出讲解中的概念漏洞、逻辑跳跃、公式条件缺失、直观含义不清和表达含混处，讲解结束后生成诊断报告，并可存入个人知识库。

## 运行方式

当前版本已经接入智谱大模型，需要通过本地服务运行：

```powershell
npm run dev
```

然后访问 `http://localhost:5173`。

如果只是打开 `index.html`，页面仍能加载，但大模型请求需要本地服务在 `5173` 端口运行。

## 环境变量

项目会从 `.env` 读取智谱配置：

```powershell
ZHIPU_API_KEY=your-zhipu-api-key
ZHIPU_MODEL=glm-5.1
ZHIPU_VISION_MODEL=glm-4.5v
PORT=5173
```

`.env` 已被 `.gitignore` 忽略，不要提交真实 API key。

## 当前实现

- 顶部两个入口：新的讲解、个人知识库。
- 三阶段学习闭环：创建讲解、讲给 AI 学生、诊断报告。
- 支持“知识点讲解”和“题目讲解”。
- 讲解输入区提供公式输入按钮，可用科学键盘生成 LaTeX 并插入讲解。
- 支持上传手写公式或草图图片，先识别成可编辑文本，再由用户确认插入讲解。
- 通过本地后端代理调用智谱大模型，前端不直接暴露 API key。
- 支持多轮补充讲解，并记录问题是否被后续解释修正。
- 最终报告包含讲解主题、理解漏洞、逻辑跳跃、已讲清楚部分、仍需复习问题和下一轮重讲任务。
- 会把当前会话和个人知识库保存到浏览器 `localStorage`，刷新页面后可继续查看。

## 大模型接入位置

当前 `server.js` 提供三个接口：

- `POST /api/chat`：根据用户最新讲解生成 AI 学生追问和漏洞观察。
- `POST /api/report`：根据完整对话生成最终诊断报告。
- `POST /api/recognize-image`：把手写公式或草图识别成可编辑文本，不直接进入对话历史。

输出仍应避免评分，保持“认真、有基础、但不会装懂的 AI 学生”口吻。
